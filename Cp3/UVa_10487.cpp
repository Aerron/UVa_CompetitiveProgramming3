#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
	int num, query, input;
	vector <int> vec;
	while(scanf("%d",&num), num){
		while(num--){
			cin>>input;
			vec.push_back(input);
		}
		sort(vec.begin(), vec.end());
		cin>>query;
		while(query--){
			cin>>input;

				
		}
	}
	return 0;
}